---
apiVersion: 1

datasources:
  - name: DS_PROMETHEUS
    type: prometheus
    uid: DS_PROMETHEUS
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    # Enable dashboard template variable substitution
    editable: false

